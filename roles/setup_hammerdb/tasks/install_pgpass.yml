---

- name: Reference Postgres variables
  ansible.builtin.include_vars: "{{ pg_type }}.yml"

- name: Install pgpass file
  ansible.builtin.template:
    dest: .pgpass
    src: pgpass.template
    mode: 0600
  become: false
  when: pg_type in ['DBaaS']
