---
- name: Get the Cluster SuperUser Secret
  shell: kubectl get secret {{ cluster_superuser_name }} -oyaml -o=jsonpath={.data.password}|base64 -d
  register: superusersecret

- name: Display the Cluster SuperUser Secret
  debug: var=superusersecret.stdout_lines