---
- name: Reload a Cluster
  shell: kubectl cnpg reload -n {{ cnpg_namespace }} {{ cluster_name }}
  register: reloadcluster

- name: Display the results of the cluster reload
  debug: var=reloadcluster.stdout_lines
